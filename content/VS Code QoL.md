**Header files not detected in C/C++**
`Ctrl`+`Shift`+`P` (Opens the _command palette_)-> "Edit Configurations (JSON)". Add the include paths under the `"includePath"` entry. I added `"/usr/include/**"` 

**Multi-cursor editing** - Use `Alt`

